{
  "name": "@sessions/core",
  "version": "0.0.0",
  "description": "Shared, agent-agnostic core for the session-explorer VS Code extensions (models, rendering, discovery orchestration, webview UI).",
  "private": true,
  "license": "MIT",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w"
  },
  "dependencies": {
    "markdown-it": "^14.1.1"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.14.10",
    "@types/vscode": "^1.90.0",
    "typescript": "^5.6.2"
  }
}
